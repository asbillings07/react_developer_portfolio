extends layout.pug

block content
    .div
        h1= `Status: ${error.status}`
        h2= `Error: ${error.message}`
        h3= `${error.stack}`
            a#img(href='/') Go Home 
            //- img.error(src='/static/images/error-img.jpg')      
            
